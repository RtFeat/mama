import 'package:flutter/material.dart';

class TrackersTable extends StatelessWidget {
  const TrackersTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
